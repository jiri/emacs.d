* Sindriava's Emacs config
  [[./Screenshot.png]]
** Installation
   Just clone this repository to your =~/.emacs.d/= folder. It will download all the necessary plugins and set itself up.

   =git clone https://github.com/sindriava/emacs.d.git ~/.emacs.d/=
